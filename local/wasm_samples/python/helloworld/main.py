# Minimal "hello world" debuglet in Python.
#
# NOTE: This sample is NOT currently runnable on the executor. See
# ../../README.md (the python/ guide) for why — there is no WASI-preview-1,
# filesystem-free Python toolchain that wazero can run today. The source is
# kept as a reference for if that changes.

print("Hello from Debuglet! (Python)")
