// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: tesla.sql

package database

import (
	"context"
	"time"
)

const createTeslaChain = `-- name: CreateTeslaChain :exec
INSERT INTO tesla_chains (
    generation,
    anchor,
    epoch_base,
    delay_ns,
    chain_length,
    created_at
) VALUES (?, ?, ?, ?, ?, ?)
`

type CreateTeslaChainParams struct {
	Generation  int64
	Anchor      []byte
	EpochBase   time.Time
	DelayNs     int64
	ChainLength int64
	CreatedAt   time.Time
}

func (q *Queries) CreateTeslaChain(ctx context.Context, arg CreateTeslaChainParams) error {
	_, err := q.db.ExecContext(ctx, createTeslaChain,
		arg.Generation,
		arg.Anchor,
		arg.EpochBase,
		arg.DelayNs,
		arg.ChainLength,
		arg.CreatedAt,
	)
	return err
}

const listTeslaChains = `-- name: ListTeslaChains :many
SELECT generation, anchor, epoch_base, delay_ns, chain_length, created_at FROM tesla_chains
ORDER BY generation
`

func (q *Queries) ListTeslaChains(ctx context.Context) ([]TeslaChain, error) {
	rows, err := q.db.QueryContext(ctx, listTeslaChains)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []TeslaChain
	for rows.Next() {
		var i TeslaChain
		if err := rows.Scan(
			&i.Generation,
			&i.Anchor,
			&i.EpochBase,
			&i.DelayNs,
			&i.ChainLength,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const nextTeslaChainGeneration = `-- name: NextTeslaChainGeneration :one
SELECT CAST(COALESCE(MAX(generation), 0) + 1 AS INTEGER) FROM tesla_chains
`

func (q *Queries) NextTeslaChainGeneration(ctx context.Context) (int64, error) {
	row := q.db.QueryRowContext(ctx, nextTeslaChainGeneration)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}
