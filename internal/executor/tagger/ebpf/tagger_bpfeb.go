// Code generated by bpf2go; DO NOT EDIT.
//go:build (mips || mips64 || ppc64 || s390x) && linux

package ebpf

import (
	"bytes"
	_ "embed"
	"fmt"
	"io"
	"structs"

	"github.com/cilium/ebpf"
)

type taggerAkEntry struct {
	_  structs.HostLayout
	K0 uint64
	K1 uint64
}

// loadTagger returns the embedded CollectionSpec for tagger.
func loadTagger() (*ebpf.CollectionSpec, error) {
	reader := bytes.NewReader(_TaggerBytes)
	spec, err := ebpf.LoadCollectionSpecFromReader(reader)
	if err != nil {
		return nil, fmt.Errorf("can't load tagger: %w", err)
	}

	return spec, err
}

// loadTaggerObjects loads tagger and converts it into a struct.
//
// The following types are suitable as obj argument:
//
//	*taggerObjects
//	*taggerPrograms
//	*taggerMaps
//
// See ebpf.CollectionSpec.LoadAndAssign documentation for details.
func loadTaggerObjects(obj interface{}, opts *ebpf.CollectionOptions) error {
	spec, err := loadTagger()
	if err != nil {
		return err
	}

	return spec.LoadAndAssign(obj, opts)
}

// taggerSpecs contains maps and programs before they are loaded into the kernel.
//
// It can be passed ebpf.CollectionSpec.Assign.
type taggerSpecs struct {
	taggerProgramSpecs
	taggerMapSpecs
	taggerVariableSpecs
}

// taggerProgramSpecs contains programs before they are loaded into the kernel.
//
// It can be passed ebpf.CollectionSpec.Assign.
type taggerProgramSpecs struct {
	DebugletTag *ebpf.ProgramSpec `ebpf:"debuglet_tag"`
}

// taggerMapSpecs contains maps before they are loaded into the kernel.
//
// It can be passed ebpf.CollectionSpec.Assign.
type taggerMapSpecs struct {
	AkMap *ebpf.MapSpec `ebpf:"ak_map"`
}

// taggerVariableSpecs contains global variables before they are loaded into the kernel.
//
// It can be passed ebpf.CollectionSpec.Assign.
type taggerVariableSpecs struct {
}

// taggerObjects contains all objects after they have been loaded into the kernel.
//
// It can be passed to loadTaggerObjects or ebpf.CollectionSpec.LoadAndAssign.
type taggerObjects struct {
	taggerPrograms
	taggerMaps
	taggerVariables
}

func (o *taggerObjects) Close() error {
	return _TaggerClose(
		&o.taggerPrograms,
		&o.taggerMaps,
	)
}

// taggerMaps contains all maps after they have been loaded into the kernel.
//
// It can be passed to loadTaggerObjects or ebpf.CollectionSpec.LoadAndAssign.
type taggerMaps struct {
	AkMap *ebpf.Map `ebpf:"ak_map"`
}

func (m *taggerMaps) Close() error {
	return _TaggerClose(
		m.AkMap,
	)
}

// taggerVariables contains all global variables after they have been loaded into the kernel.
//
// It can be passed to loadTaggerObjects or ebpf.CollectionSpec.LoadAndAssign.
type taggerVariables struct {
}

// taggerPrograms contains all programs after they have been loaded into the kernel.
//
// It can be passed to loadTaggerObjects or ebpf.CollectionSpec.LoadAndAssign.
type taggerPrograms struct {
	DebugletTag *ebpf.Program `ebpf:"debuglet_tag"`
}

func (p *taggerPrograms) Close() error {
	return _TaggerClose(
		p.DebugletTag,
	)
}

func _TaggerClose(closers ...io.Closer) error {
	for _, closer := range closers {
		if err := closer.Close(); err != nil {
			return err
		}
	}
	return nil
}

// Do not access this directly.
//
//go:embed tagger_bpfeb.o
var _TaggerBytes []byte
