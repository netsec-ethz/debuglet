---
- name: Restart debuglet services
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  loop: "{{ debuglet_services }}"
  when:
    - debuglet_manage_services | bool
    - payload_restart_services | default(true) | bool
