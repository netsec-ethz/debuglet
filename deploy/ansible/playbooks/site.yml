---
# Full deployment: dispatcher first, then all executors.
# Usage: cd deploy/ansible && ansible-playbook playbooks/site.yml

- import_playbook: deploy-dispatcher.yml
- import_playbook: deploy-executors.yml
