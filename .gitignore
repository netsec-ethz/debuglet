*.crt
*.key
*.pcap

# binaries
debuglet-executor*
debuglet-dispatcher*
tagger.o
/dispatcher
/executor
__debug*

# mise's project-local install/cache dir (see deploy/bin/mise) and the
# project-relative Go module/build caches CI caches between runs.
/deploy/.mise
/.cache

# Python env
.venv

.testCoverage.txt
engine.test
benchmarks

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Python generated files
.mypy_cache/
__pycache__
*.pyc
.venv

# Code agents
.claude

.data

# Real Ansible inventory identifies private infrastructure (dispatcher
# address, executor hostnames, SSH usernames). Copy hosts.yml.example to
# hosts.yml (prod) / hosts.dev.yml.example to hosts.dev.yml (dev) locally;
# in CI both are injected as Secure Files / protected variables. See
# deploy/ansible/hosts.yml.example and hosts.dev.yml.example.
/deploy/ansible/hosts.yml
/deploy/ansible/hosts.dev.yml
